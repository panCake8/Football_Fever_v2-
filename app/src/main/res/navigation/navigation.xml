<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/clubTablesFragment">

    <fragment
        android:id="@+id/clubTablesFragment"
        android:name="com.pancake.footballfever.ClubTablesFragment"
        android:label="fragment_club_tables"
        tools:layout="@layout/fragment_club_tables" >
        <argument
            android:name="teamId"
            app:argType="integer"
            android:defaultValue="40" />
        <argument
            android:name="season"
            app:argType="integer"
            android:defaultValue="2023" />
    </fragment>
</navigation>